spring:
  datasource:
    url: jdbc:postgresql://db:5432/order-db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
application:
  product-service-url: http://product-service-web-1:8080/products

logging:
  level:
    root: info

